import { SortValidadePipe } from './sort-validade.pipe';

describe('SortValidadePipe', () => {
  it('create an instance', () => {
    const pipe = new SortValidadePipe();
    expect(pipe).toBeTruthy();
  });
});
